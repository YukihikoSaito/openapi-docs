{
  "name": "openapi-documents",
  "description": "OpenAPI Documents",
  "version": "1.0.0",
  "scripts": {
    "watch": "http-server --cors -c-1",
    "validate": "swagger-cli validate ./openapi.json"
  },
  "dependencies": {
    "swagger-cli": "^4.0.2"
  },
  "devDependencies": {
    "http-server": "^0.12.1"
  }
}
