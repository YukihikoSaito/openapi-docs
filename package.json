{
  "name": "openapi-documents",
  "description": "OpenAPI Documents",
  "version": "1.0.0",
  "scripts": {
    "watch-redoc": "http-server ./redoc --cors -c-1",
    "validate-redoc": "swagger-cli validate ./redoc/openapi.json"
  },
  "dependencies": {
    "swagger-cli": "^4.0.2"
  },
  "devDependencies": {
    "http-server": "^0.12.1"
  }
}
